# Pathfinder Fixture Exports

Single-entry JSON exports for Pathfinder NPC and creature testing.

These are staging captures from `Wrath of Thrune` with one file per named entry.
Some files are still raw text snapshots, while others include structured stat blocks
where the PDF made them easy to isolate.

Current count: 25 entries.
